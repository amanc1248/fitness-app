/// Compare dates, return a bool value. [True] is same date or [false] else
// bool _compareDate(dateA, date) {
//   if (dateA?.month == date?.month &&
//       dateA?.day == date?.day &&
//       dateA?.year == date?.year) {
//     return true;
//   }
//   return false;
// }
